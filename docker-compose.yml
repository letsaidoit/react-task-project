version: "3.8"

services:
  react-app:
    container_name: react-task-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Uncomment below for custom nginx config
    # volumes:
    #   - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
